import VideoStreamHandler from "./pagelements/VideoStreamHandler";

const LandingPage = () => {
  return (
    <main className="h-screen w-screen overflow-hidden">
      <VideoStreamHandler></VideoStreamHandler>
    </main>
  );
};

export default LandingPage;
